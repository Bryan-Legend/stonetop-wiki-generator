# Book ARTWORK is NOT redistributable: the books' text is CC BY-SA 4.0, but
# "all artwork herein is (c) 2026 by Lucie Arnoux". Map images are artwork.
# Build without --maps and these are never produced in the first place.
Stonetop_Wiki/images/maps/

# (The extracted TEXT — pages/, index.html, previews-data.js, search-index.js —
# is CC BY-SA 4.0 and IS published; see README "License".)

# Local build bookkeeping (tells the next build which root files it owns)
Stonetop_Wiki/.build-manifest

# Legacy build folder name
Book_II_Wiki/

# Source PDFs and campaign map sheets (not redistributed)
*.pdf
Maps/
maps/
Book_II/
Book_I/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
*.egg-info/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.tmp.*

# Translation scratch: numbered segment files and the assembled bodies they
# apply to. The translation itself is i18n/pages/<code>/<slug>.json; these are
# regenerable with `python i18n/xlate.py extract <slug>`.
i18n/_work/
